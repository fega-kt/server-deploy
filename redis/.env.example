REDIS_PASSWORD=change_me_redis_password
REDIS_PORT=6379

# Tùy chọn — để trống thì dùng user `default` (requirepass kiểu cũ).
# Nếu set, user default sẽ bị tắt và chỉ user này (full quyền) được dùng.
# Nhớ cập nhật backend để gửi kèm username khi connect nếu bật cái này.
REDIS_USER=
