# Env files — chỉ commit .env.example, không commit .env thật
.env
**/.env

# Vault secrets
.vault-init.json
.vault-unseal-key
.vault-root-token
**/.vault.json

# Cloudflare credentials
cloudflared/*.json
cloudflared/config.yml

# OS
.DS_Store
Thumbs.db

# Editor
.vscode/
.idea/
